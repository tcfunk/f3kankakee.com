{% embed 'artisan:page' with {
  attributes: create_attribute({'class': ['shadow']}),
  main_attributes: create_attribute({'class': ['container', 'py-5']}),
  header: {
    top: page.header_top,
    branding: page.branding,
    primary_menu: page.primary_menu,
    secondary_menu: page.secondary_menu,
    header: page.header,
  },
  main_content: page.content,
  footer: {
    top: page.footer_top,
    bottom: page.footer_bottom,
  }
} only %}
{% endembed %}